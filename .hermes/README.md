# Hermes Agent integration

Hermes is the local planning and supervision layer for this repository. Codex remains the executor: it edits files, runs commands and returns evidence. Hermes is never a production runtime and is started manually.

Read `project-context.md`, `architecture.md`, `rules.md` and `state/project-state.json` before creating a task.
