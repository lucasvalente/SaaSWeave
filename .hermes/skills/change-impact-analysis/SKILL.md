# Change Impact Analysis

Before implementation map direct files, domains, APIs, tables, permissions, routes, i18n, tests, shared components, and downstream consumers. Consult `.hermes/state/dependency-graph.json` and emit direct impact, regression risks, and required tests. Keep analysis minimal-sufficient and preserve security boundaries.
