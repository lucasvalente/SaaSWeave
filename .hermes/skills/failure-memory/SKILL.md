# Failure Memory

Before routing, query `.hermes/state/failure-memory.json` for related domain/symptom entries and include matching regression tests and prevention rules. Record only proven failures with id, date, domain, symptom, root cause, fix, regression test, prevention rule, and related files.
