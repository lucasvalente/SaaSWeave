# Context Router

Select only relevant project state, architecture invariants, ADRs, domain docs, acceptance criteria, and source files for each task. Use broad context for HIGH/CRITICAL work and minimal-sufficient context otherwise. Stable facts may come from `.hermes/state/context-cache.json`; never cache volatile values without a version/checksum.
