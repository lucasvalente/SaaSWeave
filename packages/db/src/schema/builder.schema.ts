import { index, integer, jsonb, pgTable, text, timestamp } from "drizzle-orm/pg-core";
import { organization, user } from "#@/schema/auth.schema";
import { project } from "#@/schema/project.schema";
export const builderSession = pgTable("builder_session", { id: text("id").primaryKey(), projectId: text("project_id").notNull().references(() => project.id, { onDelete: "cascade" }), workspaceId: text("workspace_id").notNull().references(() => organization.id, { onDelete: "cascade" }), createdBy: text("created_by").notNull().references(() => user.id), status: text("status").notNull().default("idle"), createdAt: timestamp("created_at").defaultNow().notNull(), updatedAt: timestamp("updated_at").defaultNow().notNull() }, (t) => [index("builder_session_project_idx").on(t.projectId), index("builder_session_workspace_idx").on(t.workspaceId)]);
export const builderMessage = pgTable("builder_message", { id: text("id").primaryKey(), sessionId: text("session_id").notNull().references(() => builderSession.id, { onDelete: "cascade" }), role: text("role").notNull(), content: text("content").notNull(), status: text("status").notNull().default("completed"), metadata: jsonb("metadata"), createdAt: timestamp("created_at").defaultNow().notNull() }, (t) => [index("builder_message_session_idx").on(t.sessionId, t.createdAt)]);
export const builderPlan = pgTable("builder_plan", { id: text("id").primaryKey(), sessionId: text("session_id").notNull().references(() => builderSession.id, { onDelete: "cascade" }), intent: text("intent").notNull(), summary: text("summary").notNull(), steps: jsonb("steps").notNull(), createdAt: timestamp("created_at").defaultNow().notNull() });
export const builderSnapshot = pgTable("builder_snapshot", { id: text("id").primaryKey(), projectId: text("project_id").notNull().references(() => project.id, { onDelete: "cascade" }), parentId: text("parent_id"), createdBy: text("created_by").notNull().references(() => user.id), source: text("source").notNull(), summary: text("summary"), manifest: jsonb("manifest").notNull(), createdAt: timestamp("created_at").defaultNow().notNull() }, (t) => [index("builder_snapshot_project_idx").on(t.projectId, t.createdAt)]);
export const builderOperation = pgTable("builder_operation", { id: text("id").primaryKey(), sessionId: text("session_id").notNull().references(() => builderSession.id, { onDelete: "cascade" }), type: text("type").notNull(), path: text("path").notNull(), content: text("content"), order: integer("order").notNull(), status: text("status").notNull().default("pending"), error: text("error"), createdAt: timestamp("created_at").defaultNow().notNull() });
